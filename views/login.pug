doctype html
html
  head
    title Inicio de sesión
    link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css")
  body(style="display: flex; justify-content: center; align-items: center; height: 100vh; margin: 0;")
    .container(style="max-width: 400px;")
      .card
        .card-body
          h1.card-title(text-align="center") Iniciar sesión
          if message
            .alert.alert-danger(role="alert") #{message}
          form(action="/users/login", method="POST")
            .mb-3
              label(for="cedula", class="form-label") Cédula:
              input(type="text", name="cedula", class="form-control", required)
            .mb-3
              label(for="password", class="form-label") Contraseña:
              input(type="password", name="password", class="form-control", required)
            button.btn.btn-primary(type="submit", style="width: 100%") Iniciar sesión
          p.mt-3.mb-0.text-center ¿No tienes una cuenta? #[a(href="/registro") Crear cuenta]

